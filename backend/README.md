# AAAGym API

NestJS service for the AAAGym register. Run from this folder with `npm run start:dev` after creating `backend/.env` from the root `.env.example`.

See the root README for routes, environment variables, and deploy notes.
